# Enables the SCM API proposal listeners, one port per provider. Selected with FOGWALL_CONFIG_PROFILES and
# mounted by docker/docker-compose.proposals.yml; provisioning a proposal identity + grant is in
# test/otel-smoke/README.md.
providers:
  github:
    enabled: true
    proposals: { enabled: true, port: 8481, require-validated-head: false, merge-enabled: true }
  gitlab:
    enabled: true
    proposals: { enabled: true, port: 8482, require-validated-head: false, merge-enabled: true }
  codeberg:
    enabled: true
    proposals: { enabled: true, port: 8483, require-validated-head: false, merge-enabled: true }
  gitea:
    enabled: true
    uri: http://gitea:3000 # local container; set to https://gitea.com for a real repo
    proposals: { enabled: true, port: 8484, require-validated-head: false, merge-enabled: true }

proposals:
  block:
    literals:
      - "internal.corp.example.com"
