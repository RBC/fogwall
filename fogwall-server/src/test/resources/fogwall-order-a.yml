# Profile-order precedence fixture for FogwallConfigLoaderTest — sets a distinct value from fogwall-order-b.yml so a
# test can prove which profile wins when both are named in FOGWALL_CONFIG_PROFILES.
server:
  port: 9001
